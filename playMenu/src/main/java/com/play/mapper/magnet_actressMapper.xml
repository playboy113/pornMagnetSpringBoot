<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.play.mapper.magnet_actressMapper">
  <resultMap id="BaseResultMap" type="com.play.entity.magnet_actress">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Nov 22 22:22:18 CST 2022.
    -->
    <result column="id" javaType="INTEGER" property="id"/>
    <result column="actress" jdbcType="VARCHAR" property="actress" />
    <result column="num" jdbcType="VARCHAR" property="num" />

  </resultMap>
  <select id="selectForAllActressInMysql" resultType="java.lang.String">
    select distinct actress from magnet_actress;
  </select>
    <select id="selectNumByActress" resultType="java.lang.String" parameterType="map">
      select distinct num from magnet_db

        <where>
      <if test="actress != null and actress != ''">
        and actress like '%' #{actress} '%'
      </if>
        </where>

    </select>
  <select id="selectActressByNum" resultType="java.lang.String" parameterType="map">
    select actress from magnet_actress

<!--    <where>-->
<!--      <if test="num != null and num != ''">-->
<!--        and num like '%' #{num} '%'-->
<!--      </if>-->
<!--    </where>-->

  </select>
  <select id="selectNumInDB" resultType="java.lang.String">
    select distinct num from magnet_db
  </select>
  <select id="selectActressByNums" resultType="java.lang.String" parameterType="map">

    select distinct  actress from magnet_actress

    where
       num in
      <foreach collection="list" item="num" index="index" open="(" close=")" separator=",">
        #{num}
      </foreach>

  </select>
    <select id="selectAllNums" resultType="java.lang.String">
      select distinct num from magnet_db
    </select>
  <select id="selectAllNumsInLocate" resultType="java.lang.String">
    select distinct num from magnet_videos_locate
  </select>
  <select id="selectLocateByNum" resultType="java.lang.String">
    select distinct  locate from magnet_videos_locate
    where num = #{num}

  </select>


</mapper>