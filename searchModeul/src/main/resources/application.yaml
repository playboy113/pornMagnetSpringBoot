server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/magnet?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: HH:mm:ss:SSS
#mybatis-plus:
#  configuration:
#    map-underscore-to-camel-case: true
#  type-aliases-package: cn.itcast.hotel.pojo
mybatis-plus:
  mapper-locations:
    classpath: com/zhang/mapper/*.xml

